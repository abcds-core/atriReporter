% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projections.R
\name{get_demographic_projections}
\alias{get_demographic_projections}
\title{Generate Demographic Projections}
\usage{
get_demographic_projections(
  projection_no,
  ...,
  controls = FALSE,
  include_ltfu = FALSE
)
}
\arguments{
\item{projection_no}{Integer. Target total enrollment count to project onto.
Current observed proportions are preserved; cell counts are rescaled to
sum to this value.}

\item{...}{Optional parameters to filter the demographics dataset by to get
a more focused projection number (e.g., site_label == "KUMC")}

\item{controls}{Logical. If \code{TRUE}, include control subjects in the
demographic pull. Passed directly to \code{get_demographics()} and
\code{get_disposition()}. Default \code{FALSE}.}

\item{include_ltfu}{Logical. If \code{TRUE}, retain subjects lost to follow-up
(LTFU) in the counts. If \code{FALSE} (default), LTFU subjects identified via
\code{get_disposition()} are excluded before summarising.}
}
\value{
A named list with two elements:
\describe{
\item{\code{Original}}{A data frame of observed counts cross-tabulated by
ethnicity, gender, and race category, with row and column totals
appended.}
\item{\code{Projected}}{The same structure with counts rescaled to
\code{projection_no}, rounded to whole numbers.}
}
}
\description{
Retrieves current demographic data and rescales enrollment counts to a
target projection size, broken down by race, ethnicity, and gender.
}
\details{
Race categories are treated as non-exclusive in the source data; subjects
endorsing more than one race category are recoded into a synthetic
\code{"More than One Race"} category and removed from all individual race columns.

Projection scaling preserves within-column proportions:
each cell is multiplied by \code{(projection_no / column_total)}. Cells whose
column total is zero are left as zero. Note that independent rounding of
each cell may cause projected column totals to differ from \code{projection_no}
by ±1; use a largest-remainder method if exact totals are required.
}
\examples{
\dontrun{
result <- get_demographic_projections(projection_no = 500)
result$Original
result$Projected

# Include control subjects and retain LTFU
result_full <- get_demographic_projections(
  projection_no = 500,
  controls      = TRUE,
  include_ltfu  = TRUE
)
}
}
\seealso{
\code{\link[=get_demographics]{get_demographics()}}, \code{\link[=get_disposition]{get_disposition()}}
}
